# Root filesystem. M0 scaffold: single root. Next step per ADR-0001:
# split into root-a/root-b pairs with dm-verity hash partitions for
# systemd-sysupdate A/B flips.
[Partition]
Type=root
Format=btrfs
CopyFiles=/
SizeMinBytes=4G
