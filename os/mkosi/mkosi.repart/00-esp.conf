# EFI system partition — holds the signed UKI(s).
# 1G: room for two UKIs — the A/B pair (current + update) is the point
# of the layout (ADR-0001).
[Partition]
Type=esp
Format=vfat
SizeMinBytes=1G
SizeMaxBytes=1G
CopyFiles=/boot:/
CopyFiles=/efi:/
