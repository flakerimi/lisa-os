# iwd stays the Wi-Fi supplicant (the package the image shipped before
# the desktop landed); NetworkManager provides the GNOME shell
# indicator / quick-settings UI on top of it. wpa_supplicant (an NM
# package dependency) is installed but never activated with this set.
[device]
wifi.backend=iwd
