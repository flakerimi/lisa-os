# Persist the journal on the var partition so a failed boot can be
# examined from the *next* boot (and dumped to the ESP by
# lisa-boot-report.service). Default `auto` would stay volatile because
# the image ships no /var/log/journal directory.
[Journal]
Storage=persistent
