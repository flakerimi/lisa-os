# First boot on a real disk: grow /var to fill the remaining space
# (models, ledger, context live there). systemd-repart runs at boot and
# extends the last partition; on the 13G image layout this is a no-op.
[Partition]
Type=var
GrowFileSystem=yes
