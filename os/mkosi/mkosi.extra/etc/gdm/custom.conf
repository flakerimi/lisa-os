# GDM autologin into the PROVISIONAL field-test account (lisa/lisa,
# created in mkosi.postinst.chroot; see os/mkosi/README.md — real
# first-boot user setup is the M7 OOBE, PLAN §6). The password still
# guards the lock screen and sudo.
[daemon]
AutomaticLoginEnable=True
AutomaticLogin=lisa
