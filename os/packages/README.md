# os/packages — PKGBUILDs & systemd units

Spec: docs/PLAN.md §6, §5.10. Milestone: M0→M1.

PKGBUILDs for the [lisa] repo plus the hardened systemd units (PrivateNetwork=, seccomp allowlists, cgroup ceilings) whose egress guarantees the CI netns packet counter verifies.

Status: **not started** — scaffold placeholder. Read the spec section (and CLAUDE.md rules) before writing code here.
