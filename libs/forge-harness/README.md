# forge-harness — the agentic app-building loop

Spec: docs/PLAN.md §5.12.1. Milestone: M6 (walking skeleton in M0 backlog).

plan → edit files (jailed to project dir) → dart analyze/build → hot-reload preview → VLM screenshot self-inspection → iterate. Pluggable backends: local coder models or any agent CLI (Claude Code) over the same tool jail.

Status: **not started** — scaffold placeholder. Read the spec section (and CLAUDE.md rules) before writing code here.
