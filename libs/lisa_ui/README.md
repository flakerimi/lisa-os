# lisa_ui — the Lisa design system on Flutter core widgets

Spec: docs/PLAN.md §5.12. Milestone: M6 (seed in M0 backlog).

Design tokens + motion + typography reading the system theme live; AI-native widgets (LisaStreamText, ConsentChip, ContextAttachment, VoiceInput, LedgerBadge) plus the boring essentials. No material_ui/cupertino_ui dependency — core primitives only (ADR-0004).

Status: **not started** — scaffold placeholder. Read the spec section (and CLAUDE.md rules) before writing code here.
