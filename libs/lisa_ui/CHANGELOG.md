## 0.1.0

* ADR-0014 phase 1: lisa_ui becomes the kit Lisa apps import — curated
  re-export of the Material widget vocabulary, `lisaTheme` (violet seed
  `Color(0xFF6D45C9)`, Material 3, Rubik via OS-installed font), and the
  new `LisaApp` / `LisaScaffold` / `LisaCard` widgets. `LisaTokens`,
  `LisaTheme`, `LisaStreamText`, and `ConsentChip` unchanged.

## 0.0.1

* Core-widgets seed: `LisaTokens`, `LisaTheme`, `LisaStreamText`,
  `ConsentChip` (ADR-0004).
