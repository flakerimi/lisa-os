# lisa_flutter — Dart SDK bindings

Spec: docs/PLAN.md §5.12. Milestone: M6.

Mirrors liblisa (sessions, guided generation, tasks, memory, tools) over D-Bus via the dbus Dart package, OpenAI-compat endpoint as fallback transport. Blocked on the ADR-0004 spike.

Status: **not started** — scaffold placeholder. Read the spec section (and CLAUDE.md rules) before writing code here.
