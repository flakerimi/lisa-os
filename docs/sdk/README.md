# SDK documentation

Spec: docs/PLAN.md §5.6. Milestone: M2.

Quickstarts (guided generation first — the recipe-extractor sample in < 40 lines of Python), API reference, and the OpenAI-compat zero-dependency path. Also the RAG corpus the Forge harness retrieves from (§5.12.1).

Status: **not started** — scaffold placeholder. Read the spec section (and CLAUDE.md rules) before writing code here.
