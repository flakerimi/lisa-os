# Ledger app — the transparency centerpiece

Spec: docs/PLAN.md §5.7.6. Milestone: M4.

Renders the append-only audit DB: every model call, context grant, tool execution — filterable, tap-through to the full prompt envelope with provenance. Includes usage stats and grant management shortcuts.

Status: **not started** — scaffold placeholder. Read the spec section (and CLAUDE.md rules) before writing code here.
