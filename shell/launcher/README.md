# Semantic launcher & search

Spec: docs/PLAN.md §5.7.2. Milestone: M4.

One box mixing app launch, lexical+vector file hits, bus actions, and grammar-constrained calculator answers (math routes to qalc, never the model). Budgets: first results < 150 ms, semantic refinement < 700 ms.

Status: **not started** — scaffold placeholder. Read the spec section (and CLAUDE.md rules) before writing code here.
