# models/adapters — task LoRA set

Spec: docs/PLAN.md §7. Milestone: post-M4.

LoRA adapters on the system model (summarize/extract/classify/tone) hot-swapped at runtime by inferenced. Training stack choice is an open question (PLAN §13).

Status: **not started** — scaffold placeholder. Read the spec section (and CLAUDE.md rules) before writing code here.
