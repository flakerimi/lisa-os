# tests/perf — latency & throughput gates

Spec: docs/PLAN.md §11, §8. Milestone: M1+.

token/s, time-to-first-token, launcher latency, indexer jank — run on real Tier 1/2/4 reference boxes (self-hosted runners), not just QEMU.

Status: **not started** — scaffold placeholder. Read the spec section (and CLAUDE.md rules) before writing code here.
