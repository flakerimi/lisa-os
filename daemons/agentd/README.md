# lisa-agentd — system agent & MCP host

Spec: docs/PLAN.md §5.4. Milestone: M5.

Registry and client of app MCP servers; executes assistant plans under bus-enforced confirmation tiers (read/write/destructive) with an undo journal. Guardrail prompt architecture lives in Appendix C; injection suite gates merges.

Status: **not started** — scaffold placeholder. Read the spec section (and CLAUDE.md rules) before writing code here.
